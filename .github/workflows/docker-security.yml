name: Docker Security Regression

on:
  workflow_dispatch:

jobs:
  docker-security:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false
      - name: Run Docker security regression suite
        run: bash scripts/e2e/security-regression-docker.sh
